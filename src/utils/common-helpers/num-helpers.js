export const sumNum = (first, second) => +first + +second;
export const mulNum = (first, second) => +first * +second;
export const divNum = (first, second) => +first / +second;
export const substactNum = (first, second) => +first - +second;
export const fixedNum = (num, digits) => +num.toFixed(digits);
